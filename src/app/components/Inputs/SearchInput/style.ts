import styled from "@emotion/styled"
import { NormalInput } from "../NormalInput"

export const StyledSearchInput = styled(NormalInput)`
  max-width: 320px;
  .MuiInputBase-root {
    border-radius: 16px;
  }
`
